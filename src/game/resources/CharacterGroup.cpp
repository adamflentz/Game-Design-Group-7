#include "game/resources/CharacterGroup.hpp"

bool CharacterGroup::isColliding(sf::FloatRect hbox){
    for(auto a = CharacterList.begin(); a != CharacterList.end(); a++){
        return false;
    }
    return false;
}